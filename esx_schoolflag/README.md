# ESX School Flag

Capture the flag game mode for ESX using Vue.js UI.

Place this resource in your `resources` folder and ensure it in your
`server.cfg`.

Features:
- Scheduled matches based on `config.lua` times.
- Teams are defined by player jobs `school_a` and `school_b`.
- Vue.js interface for scoreboard, timer and game status.
